---
interface Props {
  id: string;
  author: string;
  url: string;
  genre: string;
}

const { id, author, url, genre } = Astro.props;
---

<div class="indent">
  <a href={url} class="white-link">{id}</a>
  <div class="info">
    Genre: {genre}
    <br />
    Author{author.includes(",") && "s"}: {author}
  </div>
</div>

<style lang="scss">
  .indent {
    padding: 0.75em 0;
  }

  a {
    font-size: 1.2em;
    margin-bottom: 2px;
  }

  .info {
    color: hsl(0, 0%, 60%);
    margin-bottom: 5px;
  }
</style>
